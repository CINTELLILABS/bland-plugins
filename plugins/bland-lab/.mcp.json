{
  "mcpServers": {
    "bland": {
      "type": "http",
      "url": "${user_config.bland_api_url}/v1/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.bland_api_key}"
      }
    }
  }
}
